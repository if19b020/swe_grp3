package businesslayer;

import org.junit.Assert;
import org.junit.jupiter.api.Test;
import static org.junit.Assert.*;
import static org.hamcrest.CoreMatchers.*;
//import static com.sun.org.apache.xerces.internal.util.PropertyState.is;

class TourPlannerManagerFactoryTest {

    @Test
    void getManager() {
        /*TourPlannerManager manager = new TourPlannerManagerFunc();
        TourPlannerManager second_manager = new TourPlannerManagerFunc();

        // both instances should be the same object as only one instance should be created
        */
    }
}